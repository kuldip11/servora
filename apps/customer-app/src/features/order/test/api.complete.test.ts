import { beforeEach,expect,it,vi } from "vitest"; const request=vi.hoisted(()=>vi.fn()); vi.mock("@/shared/api/client",()=>({request})); import { checkoutCustomerOrder,createCustomerOrder,getCustomerOrder,initiateTakeawayPayment,verifyTakeawayPayment } from "../api";
beforeEach(()=>{vi.clearAllMocks();vi.spyOn(crypto,"randomUUID").mockReturnValue("00000000-0000-4000-8000-000000000000");});
it("covers all order API wrappers",()=>{createCustomerOrder("s",{notes:"n"}); expect(request).toHaveBeenCalledWith("/api/customer/orders",expect.objectContaining({method:"POST",headers:{"X-Customer-Request-ID":"00000000-0000-4000-8000-000000000000"}}),"s"); getCustomerOrder("s","o"); checkoutCustomerOrder("s","o"); initiateTakeawayPayment("s","o"); verifyTakeawayPayment("s",{orderId:"o",razorpayOrderId:"r1",razorpayPaymentId:"r2",razorpaySignature:"r3"}); expect(request).toHaveBeenCalledTimes(5);});
