import { beforeEach,describe,expect,it,vi } from "vitest";const mocks=vi.hoisted(()=>({list:vi.fn(),create:vi.fn(),update:vi.fn(),archive:vi.fn()}));
vi.mock("elysia",async(importOriginal)=>{const actual=await importOriginal<typeof import("elysia")>();class FakeElysia{routes:any[]=[];constructor(public options?:unknown){}use(){return this}get(path:string,handler:Function,options?:unknown){this.routes.push({method:"GET",path,handler,options});return this}post(path:string,handler:Function,options?:unknown){this.routes.push({method:"POST",path,handler,options});return this}patch(path:string,handler:Function,options?:unknown){this.routes.push({method:"PATCH",path,handler,options});return this}delete(path:string,handler:Function,options?:unknown){this.routes.push({method:"DELETE",path,handler,options});return this}}return{...actual,Elysia:FakeElysia}});vi.mock("@/core/auth",()=>({requireAuthPlugin:()=>({})}));vi.mock("../tenant.controller",()=>({tenantController:mocks}));import{tenantsRouter}from"../tenant.route";const route=(m:string,p:string)=>(tenantsRouter as any).routes.find((r:any)=>r.method===m&&r.path===p);
describe("tenant routes coverage",()=>{beforeEach(()=>{vi.clearAllMocks();mocks.list.mockResolvedValue("list");mocks.create.mockResolvedValue("create");mocks.update.mockResolvedValue("update");mocks.archive.mockResolvedValue("archive")});it("executes every route",async()=>{const auth={tenantId:"t1"};expect(await route("GET","/").handler({auth})).toBe("list");const set:any={};expect(await route("POST","/").handler({auth,body:{name:"X",organizationId:"o1"},set})).toBe("create");expect(set.status).toBe(201);expect(await route("PATCH","/:id").handler({auth,params:{id:"t1"},body:{name:"Y"}})).toBe("update");expect(await route("DELETE","/:id").handler({auth,params:{id:"t1"}})).toBe("archive");expect((tenantsRouter as any).routes).toHaveLength(4);});});
