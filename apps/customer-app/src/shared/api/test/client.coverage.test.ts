import { beforeEach,describe,expect,it,vi } from "vitest";
describe("request",()=>{beforeEach(()=>vi.resetModules());
 it("merges headers and returns data",async()=>{const fetch=vi.fn().mockResolvedValue({ok:true,json:vi.fn().mockResolvedValue({success:true,data:{x:1}})});vi.stubGlobal("fetch",fetch);const {request}=await import("../client");await expect(request("/x",{headers:{A:"b"}},"tok")).resolves.toEqual({x:1});expect(fetch).toHaveBeenCalledWith(expect.stringContaining("/x"),expect.objectContaining({headers:expect.objectContaining({"Content-Type":"application/json","X-Customer-Session":"tok",A:"b"})}));});
 it("handles no token/no init, API errors, HTTP errors and invalid JSON",async()=>{const fetch=vi.fn();vi.stubGlobal("fetch",fetch);const {request}=await import("../client");fetch.mockResolvedValueOnce({ok:true,json:vi.fn().mockResolvedValue({data:1})});await expect(request("/ok")).resolves.toBe(1);fetch.mockResolvedValueOnce({ok:true,json:vi.fn().mockResolvedValue({success:false,message:"bad"})});await expect(request("/bad")).rejects.toThrow("bad");fetch.mockResolvedValueOnce({ok:false,json:vi.fn().mockResolvedValue({})});await expect(request("/http")).rejects.toThrow("Customer API request failed");fetch.mockResolvedValueOnce({ok:false,json:vi.fn().mockRejectedValue(new Error("json"))});await expect(request("/json")).rejects.toThrow("Customer API request failed");});
});
